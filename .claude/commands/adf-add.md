---
description: Execute the prepared ADF stage script (`git add` only — no commit). Agent must have prepped /tmp/adf-add.sh first by inspecting ADF status and selecting which files to stage.
---

!`bash /tmp/adf-add.sh`
