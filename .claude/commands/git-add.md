---
description: Execute the prepared ArhuGula stage script (`git add` only — no commit). Agent must have prepped /tmp/arhugula-add.sh first by inspecting status and selecting which files to stage.
---

!`bash /tmp/arhugula-add.sh`
